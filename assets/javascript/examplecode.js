//TO START:
//CREATE MY ALPHABETCHOICES IN AN ARRAY

//NEED A VARIABLE
//NEED A FUNCTION
//NEED TO CALL THE FUNCTION


//Will Need from html pseudocode: 
// •	Game counters
// •	Word to Guess (blanks and guessed word).
// •	Wordblanks will be replaced with letters as the user             guesses / id="word-blanks"
// •	list of Wrong User Guesses / id="wrong-guesses"
// •	***Need to create id="userGuesses" in html???**
// •	guessesLeft / id="guesses-left"
// •	winCounter / id="win-counter"
// •	lossCounter / id="loss-counter"

// // THEME ARRAY: Student from class: 
// var studentName = [Janet, Brian, Justin, Ryan, Michael, Steven, Robert, Nicole, Larry, Kurtis, Elizabeth]

// What I will need:
// create a variable for each counter and assign it a               starting number and create string.

//COUNTERS
//var userGuesses = 0;
//var wrongGuesses = 0;
//var guessesLeft = 0;
//var wins = 0;
//var losses=0;

//This function is run whenever the user presses a key.
   // document.onkeyup = function(event) {
        
 //Determines which key was pressed.
  // var userGuess = event.key;

// Randomly chooses a choice from the options array. This is the Computer's guess.
// var computerGuess = computerChoices[Math.floor(Math.random() * computerChoices.length)];

//grabbing a reference to the <span>
//var userText = document.getElementById("user-text");

//Next, we give JavaScript a function to execute when onkeyup event fires.
//document.onkeyup = function(event) {
// userText.textContent = event.key;
//};












// function declaration .. up to 3-4 params best practice
//function functionName(params) {
//    return "stuff"
}

//FOR EACH LOOP
//studentName.forEach(element) {
//    console.log(element)
//});

// Looping through each item in the array and logging a message to the console.
//for (var i = 0; i <studentName.length; i++) {
    //DON'T WANT TO CONSOLE IT OR ALERT("... " + studentName[i]);
  

//WILL I NEED THIS FOR MY ARRAY?
//console.log(studentNames[0])

 // Reworked our code from last step to use "else if" instead of lots of if statements.

//NOT SURE IF WILL NEED FOR THIS GAME?
// This logic determines the outcome of the game (win/loss/tie), and increments the appropriate number

// if ((userGuess === "r") || (userGuess === "p") || (userGuess === "s")) {

//     if ((userGuess === "r") && (computerGuess === "s")) {
//         wins++;
//     }   else if ((userGuess === "r") && (computerGuess === "p")) {
//         losses++;
//     }   else if ((userGuess === "s") && (computerGuess === "r")) {
//         losses++;











